function UserProfileCard({ user, theme, action }) {}

const ComplexProp = () => {
  const user: {
    user: {};
    theme: {};
    actions: {};
  };

  return <div>ComplexProp</div>;
};

export default ComplexProp;
